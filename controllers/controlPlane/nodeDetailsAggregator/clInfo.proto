syntax = "proto3";

service CLInfoService {

  rpc SendCLinfoDetails(CLInfo) returns (ConfirmationMsg);

}

message CLInfo {
  string worker_id = 1;
  string job_result = 2;
  string job_id = 3;
}

message ConfirmationMsg {
  string status = 1;
  string node_ID = 2;
}