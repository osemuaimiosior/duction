step 1. 

docker build -t nodeDetails .

step 2. 

Docker Hub

docker tag nodeDetails yourdockerhub/nodeDetails
docker push yourdockerhub/nodeDetails

Private registry

registry.yourdomain.com/nodeDetails

///user install

step 5. Build your Monte Carlo node image
    docker pull yourdockerhub/nodeDetails

step 6. Run the compute node
    docker run -d \
    --restart unless-stopped \
    --gpus all \
    --name nodeDetails \
    yourdockerhub/nodeDetails