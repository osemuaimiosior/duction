step 1. 

docker build -t mcopencl .

step 2. 

Docker Hub

docker tag mcopencl osemu/mcopencl
docker push osemu/mcopencl

Private registry

registry.yourdomain.com/mcopencl

step 6. Run the compute node genral
    docker run -d \
    --restart unless-stopped \
    --name mcopencl \
    osemu/mcopencl