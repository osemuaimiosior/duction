syntax = "proto3";

package registry;

service Registry {

    rpc checkNodeDetails(Details) returns (StatusMessage) {}      
}

message Details {
    string NODE_CODE = 1;
    string HOST_NAME = 2;
}

message StatusMessage {
    string message = 1;
    int32 details = 2;
}